# Core app — lean build for free-tier Hugging Face Spaces (CPU)
streamlit>=1.30
pandas>=2.0
altair>=5.0
requests>=2.31

# IBM watsonx.ai — Granite analysis backend
ibm-watsonx-ai>=1.0

# NOTE: docling is intentionally omitted here.
# It pulls in PyTorch + CUDA (~2 GB), which is far too heavy for a free CPU
# Space and isn't needed for the core app. The scouting-PDF feature degrades
# gracefully when docling isn't installed. To enable that feature locally,
# run:  pip install docling